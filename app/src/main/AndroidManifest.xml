<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.intel.medicine">

    <!-- 필수 권한 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Android 13+ (API 33+) 미디어 권한 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Android 12 이하 (API 32 이하) 호환 권한 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 카메라 하드웨어 기능 선언 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <!-- 카메라 기능이 주요 기능이므로 필수로 설정 -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Medicine"
        tools:targetApi="31">

        <!-- 앱 시작 액티비티 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 앱 화면들 -->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:launchMode="singleTop" />

        <activity
            android:name=".ui.camera.CameraActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ui.result.ResultActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.camera.CameraActivity" />

        <activity
            android:name=".ui.list.MyListActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.home.HomeActivity" />

        <activity
            android:name=".ui.list.DetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.list.MyListActivity" />

        <activity
            android:name=".ui.alarm.AlarmListActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.home.HomeActivity" />

        <activity
            android:name=".ui.alarm.AlarmSettingActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.alarm.AlarmListActivity" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Medicine.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.home.HomeActivity" />

        <!-- 이미지 공유를 위한 FileProvider (필요 시 활성화) -->
        <!--
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        -->

        <!-- 알람 기능을 위한 리시버 (구현 후 활성화) -->
        <!--
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>
        -->

        <!-- 알람 서비스 (구현 후 활성화) -->
        <!--
        <service
            android:name=".alarm.AlarmService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="none" />
        -->

    </application>

</manifest>